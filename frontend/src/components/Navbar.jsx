// ARTURO: Navbar Component
// TODO: Create navigation bar that appears on all pages
// Should display:
// - Store logo/name (link to home)
// - Products link
// - If authenticated: Cart link, Admin link (if admin), user name, Logout button
// - If not authenticated: Login and Sign Up links
// Use useAuth hook to get current user
// Use useNavigate to handle logout redirect

// Import necessary dependencies
// Get user from useAuth
// Return navigation bar JSX
